# ⚠️ NOTE: This Dockerfile is kept for reference only
# The actual Dockerfile used by Railway is in the project root directory

# All backend files have been moved to the root directory:
# - package.json, package-lock.json
# - prisma/ directory
# - src/ directory  
# - tsconfig.json and other config files

# Please refer to /Dockerfile in the project root for the active build configuration
