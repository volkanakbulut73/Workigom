# =====================================
# Workigom Backend Environment Variables
# =====================================
# Copy this file to .env and update the values
# NEVER commit .env file to version control!

# =====================================
# Server Configuration
# =====================================
NODE_ENV=development
PORT=3001

# =====================================
# Database Configuration
# =====================================
# Local Development (PostgreSQL)
DATABASE_URL="postgresql://workigom_user:workigom_password@localhost:5432/workigom_db?schema=public"

# Render Production (example - will be auto-configured)
# DATABASE_URL="postgresql://user:password@host.region.render.com:5432/database?schema=public"

# =====================================
# JWT Configuration
# =====================================
# IMPORTANT: Change these secrets in production!
# Generate secure secrets: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here_change_in_production_use_64_chars_minimum
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_here_change_in_production_use_64_chars_minimum
JWT_REFRESH_EXPIRES_IN=30d

# =====================================
# CORS Configuration
# =====================================
# For development (single origin):
CORS_ORIGIN=http://localhost:5173

# For production (comma-separated list of allowed origins):
# CORS_ORIGIN=https://workigom-frontend.onrender.com,https://workigom.vercel.app,https://yourdomain.com

# =====================================
# Rate Limiting Configuration
# =====================================
# Window: 15 minutes (900000 ms)
RATE_LIMIT_WINDOW_MS=900000
# Max requests per window per IP
RATE_LIMIT_MAX_REQUESTS=100

# =====================================
# File Upload Configuration
# =====================================
# Max file size: 5MB (5242880 bytes)
MAX_FILE_SIZE=5242880
# Allowed file types (comma-separated MIME types)
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/jpg,application/pdf

# =====================================
# Email Configuration (Optional)
# =====================================
# Used for email verification and notifications
# Currently not implemented but prepared for future use
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_or_api_key
EMAIL_FROM=noreply@workigom.com

# =====================================
# Security & Debugging (Production)
# =====================================
# LOG_LEVEL=info
# TRUST_PROXY=true
# SESSION_SECRET=your_session_secret_if_using_sessions

# =====================================
# External Services (Future)
# =====================================
# CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud
# CLOUDINARY_API_KEY=your_cloudinary_key
# CLOUDINARY_API_SECRET=your_cloudinary_secret
# REDIS_URL=redis://localhost:6379
# STRIPE_SECRET_KEY=your_stripe_secret_key
